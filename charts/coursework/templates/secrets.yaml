apiVersion: v1
kind: Secret # Secret с доступами к БД.
metadata:
  name: coursework-db-secret
data:
  username: {{ .Values.environment.DB_USER| b64enc }}
  password: {{ .Values.environment.DB_PASSWORD| b64enc }}
type: kubernetes.io/basic-auth